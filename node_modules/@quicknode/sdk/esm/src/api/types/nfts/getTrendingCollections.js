import { paginationParams, supportedChainInput } from '../../../lib/validation/validators.js';

const nftTrendingCollectionsValidator = paginationParams
    .merge(supportedChainInput)
    .strict();

export { nftTrendingCollectionsValidator };
