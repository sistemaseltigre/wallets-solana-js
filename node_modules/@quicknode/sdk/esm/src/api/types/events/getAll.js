import { baseEventsInput, supportedChainInput } from '../../../lib/validation/validators.js';

const allEventsValidator = baseEventsInput
    .merge(supportedChainInput)
    .strict();

export { allEventsValidator };
