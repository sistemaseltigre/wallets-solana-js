const emptyPageInfo = {
    endCursor: null,
    hasNextPage: false,
    hasPreviousPage: false,
    startCursor: null,
};
function weiToGwei(wei) {
    return +(wei / 1e9).toFixed(2);
}

export { emptyPageInfo, weiToGwei };
